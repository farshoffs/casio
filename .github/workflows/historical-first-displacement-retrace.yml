name: Historical First Displacement Retrace

on:
  push:
    branches:
      - research/historical-2017-2020
    paths:
      - 'casio/historical_first_displacement_retrace.py'
      - '.github/workflows/historical-first-displacement-retrace.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: pip install -r requirements.txt
      - name: Run displacement retrace search
        run: |
          python -m casio.historical_first_displacement_retrace \
            --data data/xauusd_m5_secondary_octafx_mt4.csv \
            --output reports/historical-first-displacement-retrace
      - name: Publish summary
        run: cat reports/historical-first-displacement-retrace/REPORT.md >> "$GITHUB_STEP_SUMMARY"
      - uses: actions/upload-artifact@v4
        with:
          name: historical-first-displacement-retrace
          path: reports/historical-first-displacement-retrace/
          retention-days: 30
