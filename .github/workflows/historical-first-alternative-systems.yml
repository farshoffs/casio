name: Historical First Alternative Systems

on:
  push:
    branches:
      - research/historical-2017-2020
    paths:
      - 'casio/historical_first_alternative_systems.py'
      - '.github/workflows/historical-first-alternative-systems.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: pip install -r requirements.txt
      - name: Run alternative systems development
        run: |
          python -m casio.historical_first_alternative_systems \
            --data data/xauusd_m5_secondary_octafx_mt4.csv \
            --output reports/historical-first-alternative-systems
      - name: Publish summary
        run: cat reports/historical-first-alternative-systems/REPORT.md >> "$GITHUB_STEP_SUMMARY"
      - uses: actions/upload-artifact@v4
        with:
          name: historical-first-alternative-systems
          path: reports/historical-first-alternative-systems/
          retention-days: 30
