name: Historical First Ultra Deep

on:
  push:
    branches:
      - research/historical-2017-2020
    paths:
      - 'casio/historical_first_ultra_deep.py'
      - '.github/workflows/historical-first-ultra-deep.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: pip install -r requirements.txt
      - name: Run ultra-deep search
        run: |
          python -m casio.historical_first_ultra_deep \
            --data data/xauusd_m5_secondary_octafx_mt4.csv \
            --output reports/historical-first-ultra-deep
      - name: Publish summary
        run: cat reports/historical-first-ultra-deep/REPORT.md >> "$GITHUB_STEP_SUMMARY"
      - uses: actions/upload-artifact@v4
        with:
          name: historical-first-ultra-deep
          path: reports/historical-first-ultra-deep/
          retention-days: 30
