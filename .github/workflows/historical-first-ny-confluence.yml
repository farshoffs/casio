name: Historical First NY Confluence

on:
  push:
    branches:
      - research/historical-2017-2020
    paths:
      - 'casio/historical_first_ny_confluence.py'
      - '.github/workflows/historical-first-ny-confluence.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: pip install -r requirements.txt
      - name: Run NY confluence search
        run: |
          python -m casio.historical_first_ny_confluence \
            --data data/xauusd_m5_secondary_octafx_mt4.csv \
            --output reports/historical-first-ny-confluence
      - name: Publish summary
        run: cat reports/historical-first-ny-confluence/REPORT.md >> "$GITHUB_STEP_SUMMARY"
      - uses: actions/upload-artifact@v4
        with:
          name: historical-first-ny-confluence
          path: reports/historical-first-ny-confluence/
          retention-days: 30
