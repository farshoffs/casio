name: Historical First Frequency Expansion

on:
  push:
    branches:
      - research/historical-2017-2020
    paths:
      - 'casio/historical_first_frequency.py'
      - '.github/workflows/historical-first-frequency.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: pip install -r requirements.txt
      - name: Run aligned frequency expansion
        run: |
          python -m casio.historical_first_frequency \
            --data data/xauusd_m5_secondary_octafx_mt4.csv \
            --output reports/historical-first-frequency
      - name: Publish summary
        run: cat reports/historical-first-frequency/REPORT.md >> "$GITHUB_STEP_SUMMARY"
      - uses: actions/upload-artifact@v4
        with:
          name: historical-first-frequency
          path: reports/historical-first-frequency/
          retention-days: 30
