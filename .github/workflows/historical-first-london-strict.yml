name: Historical First London Strict

on:
  push:
    branches:
      - research/historical-2017-2020
    paths:
      - 'casio/historical_first_london_strict.py'
      - '.github/workflows/historical-first-london-strict.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: pip install -r requirements.txt
      - name: Run London strict search
        run: |
          python -m casio.historical_first_london_strict \
            --data data/xauusd_m5_secondary_octafx_mt4.csv \
            --output reports/historical-first-london
      - name: Publish summary
        run: cat reports/historical-first-london/REPORT.md >> "$GITHUB_STEP_SUMMARY"
      - uses: actions/upload-artifact@v4
        with:
          name: historical-first-london
          path: reports/historical-first-london/
          retention-days: 30
