name: CASIO Regime Router — Cross Diagnostics

on:
  push:
    branches: [feature/regime-router-dashboard-research]
    paths:
      - 'casio/regime_router_cross_diagnostics.py'
      - '.github/workflows/regime-router-cross-diagnostics.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/regime-router-dashboard-research
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install tabulate
      - name: Run cross diagnostics
        run: python -m casio.regime_router_cross_diagnostics
      - name: Commit report
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/regime-router-2024-cross-diagnostics
          if git diff --cached --quiet; then
            echo "No report changes"
          else
            git commit -m "research: add 2024 Regime Router cross diagnostics"
            git push origin HEAD:feature/regime-router-dashboard-research
          fi
