name: Finotive Prop Research

on:
  push:
    branches:
      - research/finotive-prop
    paths:
      - 'casio/finotive_prop_research.py'
      - 'casio/finotive_prop_hunt2.py'
      - '.github/workflows/finotive-prop-research.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: finotive-prop-research
  cancel-in-progress: true

jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Validate modules
        run: python -m compileall -q casio

      - name: Run prop hunt 2
        run: |
          python -m casio.finotive_prop_hunt2             --data data/xauusd_m5.csv             --output reports/finotive-prop-hunt2

      - name: Publish report
        if: always()
        run: |
          if [ -f reports/finotive-prop-hunt2/REPORT.md ]; then
            cat reports/finotive-prop-hunt2/REPORT.md
            cat reports/finotive-prop-hunt2/REPORT.md >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: finotive-prop-hunt2
          path: reports/finotive-prop-hunt2/
          retention-days: 30
